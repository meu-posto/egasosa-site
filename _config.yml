# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Egasosa, a melhor forma de economizar na hora de abastecer
email: contato@egasosa.com
description: >- # this means to ignore newlines until "baseurl:"
  egasosa, a melhor forma de economizar na hora de abastecer
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://www.egasosa.com.br" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: egasosaapp
github_username:  egasosa
keywords: economia, combustível, gasolina, etanol, diesel

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
  - jekyll-assets

# Assets
assets:
  compress:
    css: true
    js: true
  cache: false
  prefix: "/assets"
  digest: true
  sources:
    - _assets/images
    - _assets/javascripts
    - _assets/stylesheets
  features:
    liquid: true
    automatic_img_size: false
    automatic_img_alt: true

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
  - config.ru
  - circle.yml
